import 'package:flutter/material.dart';

void main(){
  runApp(MyProgram());
}

class MyProgram extends StatelessWidget{
  Widget build(BuildContext context){
    return MaterialApp(
      home: Scaffold(
        
        appBar: AppBar(title: Text('MI PROGRAMA')), 
        body: Column(
          children: [
          
          ],
        )
      ),
    );
  }
}