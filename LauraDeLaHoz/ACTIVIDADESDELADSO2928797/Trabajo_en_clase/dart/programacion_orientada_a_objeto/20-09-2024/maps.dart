/*import 'petición.dart';
import 'dart:convert';

class Desconocida {
  int? userId;
  int? id;
   String? title;
   String? body;
 
  Desconocida(Map decodificar){
    this.userId = decodificar['userId'];
    this.id = decodificar['id'];
    this.title = decodificar['title'];
    this.body = decodificar['body'];
  
  }

    bool statusCode(){
    return true;
  }
}

  void main(){
  Map<String, dynamic> decodificar = jsonDecode(info);

  }
  */